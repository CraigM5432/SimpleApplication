const assert = require("assert");

describe("SimpleApplication basic test", function () {
  it("should confirm that the test framework is working", function () {
    assert.strictEqual(1 + 1, 2);
  });
});
